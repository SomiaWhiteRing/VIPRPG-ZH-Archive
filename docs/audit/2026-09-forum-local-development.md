# 2026 年 9 月论坛本地开发记录

本文从原论坛设计与展示数据文档移存一次性记录，保留当时的操作范围和验收陈述；本次文档整理没有重新验证这些历史结果。以下内容不是新环境初始化步骤，也不适用于共享或远端数据库。

## 图片模型切换时的操作边界

新库顺序应用 0003_forum_images.sql 与 0004_forum_inline_images.sql，复用已有 R2 binding，无需图片服务密钥。本次旧图片为可丢弃本地开发数据：先备份并核对 schema／迁移登记，仅重建图片表、清除旧图片关联及校正图片迁移登记，保留帖子正文和其他业务数据。不得整库 reset；不自动迁移共享或远端数据，不下载或删除旧远端图片。浏览器画质、真实手机内存与交互需单独验收，构建和静态检查不代表已完成这些验证。

## 展示数据来源及当次验收记录

开发库上次基础 seed 写入时间为 2026-09-11 00:58 UTC。此次补充对照了 `869e339` 的人物署名、作品维护和账户生命周期，以及之后的作者唯一性、角色身份、发布日期、细分权限和更多信息提交（`e1cd639`、`be6be8b`、`efeedde`、`7349c4e`、`72c3498`）。角色及脸图继续使用当前词典与素材清单。

论坛数据来自本地 `output/ux-review-20260912/mock.sql` 的初始编写记录。可复用的数据已提取至 `data/dev/forum.json`，只保留论坛内容，重新映射到开发账号和编号，不复制审阅环境的账号、密码、登录会话或浏览器操作后的状态。保留原始 36 个主题、67 个楼层、54 条楼中楼、12 个 TAG 和待处理举报，并补充点赞、表情及两个已处理举报场景。2026-09-14 论坛图片切换 R2 时，独立审阅环境清除了 2 条旧图片记录并重建图片表，其余帖子正文及业务数据保留；开发库原图片表为空，同步推进到 R2 schema。两处本地数据库均在操作前备份。

这次验收只检查相关脚本语法与 lint、数据约束、公开作品条件、分页所需数量、权限模板和素材引用，以及重复执行的稳定性。没有新增测试代码，也不运行浏览器流程或完整回归。
