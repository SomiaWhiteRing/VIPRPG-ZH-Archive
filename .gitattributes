# Auto detect text files and perform LF normalization
* text=auto

# Runtime bundles and their recorded hashes must retain the imported bytes.
public/play/runtime/easyrpg/** -text

# Seed objects are original R2 bytes, including extensionless JSON/text files.
data/local-seed/objects/* -text
data/local-seed/*.gz -text
