{
  "name": "viprpg-status",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "migrate": "wrangler d1 migrations apply STATUS_DB --remote --config wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260915.1",
    "typescript": "6.0.3",
    "wrangler": "4.132.0"
  },
  "engines": {
    "node": ">=24.0.0 <25"
  }
}
